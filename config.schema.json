{
  "pluginAlias": "HAVirtualDevices",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "strictValidation": true,
  "schema": {
    "type": "object",
    "required": [
      "name",
      "haUrl",
      "token"
    ],
    "additionalProperties": false,
    "properties": {
      "name": {
        "title": "Nom",
        "type": "string",
        "default": "HA Virtual Devices",
        "description": "Nom de la plateforme affiché dans Homebridge."
      },
      "haUrl": {
        "title": "Adresse de Home Assistant",
        "type": "string",
        "format": "uri",
        "minLength": 1,
        "default": "http://homeassistant.local:8123",
        "description": "Adresse complète de Home Assistant, sans barre oblique finale."
      },
      "token": {
        "title": "Jeton d'accès longue durée",
        "type": "string",
        "format": "password",
        "minLength": 1,
        "description": "Jeton d'accès longue durée créé dans le profil Home Assistant."
      },
      "ignoredDevices": {
        "title": "Appareils à ignorer",
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [],
        "description": "Noms ou identifiants Home Assistant des appareils à ne pas publier dans HomeKit."
      },
      "debug": {
        "title": "Activer les journaux détaillés",
        "type": "boolean",
        "default": false,
        "description": "Affiche des informations techniques supplémentaires dans les journaux Homebridge."
      }
    }
  }
}